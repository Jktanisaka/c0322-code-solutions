/* exported getValue */
/*
create new function with the parameters object and key
use the provided key to get the corresponding value from the object and
return the value
*/
function getValue(object, key) {
  return object[key];
}
