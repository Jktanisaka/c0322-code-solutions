/* exported setValue */
/*
create a new function with the parameters object, key, and value
assign the value to the key of the object
*/
function setValue(object, key, value) {
  object[key] = value;
}
