/* exported last */
/*
create new function with parameter array (is an array with values)
return the last value in the array with the return operator and array.length-1

*/
function last(array) {
  return array[array.length - 1];
}
